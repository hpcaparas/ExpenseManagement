// Tune to your needs (can also be read from env)
export const IDLE_MINUTES = 15;      // total idle time before auto logout
export const WARNING_SECONDS = 10;   // show warning this many seconds before logout
export const COUNT_WHEN_HIDDEN = true;
